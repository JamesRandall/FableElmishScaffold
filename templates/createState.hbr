module {{{rootNamespace}}}.Create.State

open Elmish
open Types

let init () =
  Model.Empty, Cmd.none

let update context msg model =
  match msg with
  | ChangeAProperty newValue -> { model with AProperty = newValue }, Cmd.none
  | Submit -> model, Cmd.none